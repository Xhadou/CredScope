# Project Configuration
project:
  name: "CredScope"
  version: "1.0.0"
  random_seed: 42

# Data paths
data:
  raw_path: "data/raw"
  processed_path: "data/processed"
  features_path: "data/features"
  
# Model settings
model:
  target_column: "TARGET"
  test_size: 0.2
  validation_size: 0.1
  
# MLflow
mlflow:
  tracking_uri: "file:./mlruns"
  experiment_name: "credscope_baseline"